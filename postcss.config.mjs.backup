// postcss.config.mjs (ES module format)
import tailwindcss from 'tailwindcss';  // Correct import: Just 'tailwindcss'
import autoprefixer from 'autoprefixer'; // Optional: For browser compatibility

export default {
  plugins: [
    tailwindcss(),  // Loads Tailwind properly
    autoprefixer(), // Adds prefixes (e.g., for gradients in older browsers)
  ],
};
